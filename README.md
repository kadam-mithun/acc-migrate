# acc-migrate
ACC's Databricks → AWS-native migration accelerator (Iceberg on S3, Glue, Lake Formation) with an agentic layer on Bedrock AgentCore.
Start with `CLAUDE.md`, then `PROMPTS.md`. Specs live in `servers/*/SPEC.md`. Plan and design in `docs/`.
