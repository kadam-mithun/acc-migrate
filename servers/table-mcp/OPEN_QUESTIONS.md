# Open questions — table-mcp

| # | Raised | Question | Owner | Status |
|---|---|---|---|---|
| 1 | 2026-09-16 | Iceberg V3 `variant` support GA dates in Athena and Redshift — decides when `VARIANT` leaves S7 | Lead architect | Open (v1.1) |
| 2 | 2026-09-16 | Iceberg equivalent for Delta `INTERVAL` — none today; confirm no fixture/client tables need it beyond S7 | Lead architect | **Resolved 2026-09-20 — SPEC v0.3.2 §14 row 28.** Closed: no fixture uses `INTERVAL`; it remains S7 with reason `unsupported_type` (`ManualReason.UNSUPPORTED_TYPE`), and `assess-mcp` counts occurrences per estate so a client exception surfaces at assessment, not at conversion. |
| 3 | 2026-09-16 | S1 with transformed partitions once `add_files` supports non-identity specs — track Iceberg/PyIceberg releases | WS-B engineer | Open (v1.1) |
| 4 | 2026-09-16 | Rollback of a PROMOTED table (currently out of scope; `ALREADY_PROMOTED`) — design for v1.1 | Lead architect | Open (v1.1) |
| 5 | 2026-09-16 | §3 types `recommend_strategy(profile)` as taking only the profile, but §5 makes several rules option-dependent (S2 needs `history_days`/`history_versions`; `timestamp_mode = "ntz_utc"` forces S3; `relayout`). The scaffold carries `options` alongside the profile on `RecommendStrategyInput`, defaulted to the Gate 0 #1/#2 no-side-effect defaults (no history, no re-layout), so the signature in §3 still works unchanged. Confirm the signature or amend §3. | Lead architect | **Resolved 2026-09-20 — SPEC v0.3.2 §14 row 20.** Signature amended to `recommend_strategy(profile, options)`; `options` carries the decision-affecting settings and defaults to the no-side-effect values. `RecommendStrategyInput` already matched and now cites §3 rather than this question. |
| 6 | 2026-09-16 | §3 says `discover_tables` returns `TableRef[]` "with location, format details, size, last modified, managed/external, DLT-managed flag", while the same `TableRef` is an input to every table-scoped tool. The scaffold keeps one type: identity fields required, discovery attributes optional and `None` when the ref is used as an identifier. Confirm, or split into `TableRef` and `DiscoveredTable`. | Lead architect | **Resolved 2026-09-20 — SPEC v0.3.2 §14 row 21.** Split: `TableRef` is frozen and identity-only (and therefore hashable), taken by every table-scoped tool; `DiscoveredTable` = `TableRef` + attributes, returned only by `discover_tables`. No tool can receive a half-populated ref. |
| 7 | 2026-09-16 | §13 lists no orchestration module for `execute_conversion` / `rollback_table`. The scaffold puts both in `convert/__init__.py` alongside `COMMIT_ORDER`, keeping `promote.py` (two-reviewer file) limited to promotion. Confirm placement. | WS-B engineer | **Resolved 2026-09-20 — SPEC v0.3.2 §14 row 22.** Confirmed and added to the §13 layout: `convert/__init__.py` holds `execute_conversion`/`rollback_table` and `COMMIT_ORDER`; `promote.py` stays promotion-only, and an import-linter contract now enforces that it cannot reach into conversion mechanics. |
| 8 | 2026-09-16 | `TableRef.catalog/schema/name` are constrained to `^[A-Za-z0-9_-]{1,255}$` so a ref always composes into a valid FQN and no identifier can carry a quote, dot or newline into UC SQL, a Glue name, an S3 key or an OTel attribute. Unity Catalog permits other characters in backtick-quoted names. Confirm no in-scope client estate uses them, or agree a quoting rule. | Lead architect | **Resolved 2026-09-20 — SPEC v0.3.2 §14 row 23.** Strict pattern retained. A non-conforming identifier is no longer a validation error that drops the table: `discover.classify_identifier` reports it as `UnsupportedTable` (S7, reason `UNSUPPORTED_IDENTIFIER`) in `DiscoverTablesOutput.unsupported`, for `assess-mcp` to count. Backtick-quoted identifiers are v1.1. |
| 9 | 2026-09-16 | §3 says `get_conversion_record` "returns the evidence record", but §8 and root rule 4 make the column aggregates data values; a tool response reaches the agent context and the span payload. The scaffold returns the record with `column_aggregates` always stripped (enforced on the output model), leaving `validation.checksum_vector_hash` and the match flags. Confirm, or define a separate approved path for an auditor to read aggregates. | Security lead | **Resolved 2026-09-20 — SPEC v0.3.2 §14 row 24.** Confirmed stripped, enforced on `GetConversionRecordOutput`. The auditor path is `acc-migrate evidence show --table <ref> --include-aggregates`, reading the encrypted evidence object under a separate auditor IAM role; there is no MCP, agent or log path to aggregates. |
| 10 | 2026-09-16 | §13 lists no `errors.py`, yet the spec-named refusals (`APPROVAL_REQUIRED`, `APPROVAL_INVALID`, `ALREADY_PROMOTED`, `LOCKED`, `KMS_KEY_REQUIRED`, `WRITE_GRANT_PRESENT`) must keep their codes rather than collapse into `INTERNAL`. `TableMcpError` currently lives in `schemas.py` beside `ErrorCode`/`ErrorEnvelope`. Confirm, or add `errors.py` to the §13 layout. | WS-B engineer | **Resolved 2026-09-20 — SPEC v0.3.2 §14 row 25.** `errors.py` added to the §13 layout and created: `TableMcpError` plus a typed subclass per spec-named code, each carrying its `ErrorCode` so a refusal never collapses into `INTERNAL`. |
| 11 | 2026-09-16 | `structlog` was a declared dependency that nothing imported, so "all log emission passes through `redact.py`" (§10) was documentation, not a mechanism. | WS-B engineer | **Resolved 2026-09-20** — two parts. **Mechanism:** `redact.configure_logging` installs `redact_event` (a loggable-key allow-list) as the last processor before the JSON renderer, `get_logger` is the only sanctioned logger, and `server.main` configures it at startup; paths stay withheld until `redact_path` lands and `format_exc_info` is deliberately absent. **Enforcement:** (a) `print` → ruff `T20`; (b) stdlib `logging` → ruff `TID251` banned-api, exempt only for `redact.py`, which imports the level constant and never emits; (c) non-static event strings (f-string, `%`, `.format()`, `+`) → `tools/check_log_calls.py`, an AST check run per server in CI, since ruff cannot express it for structlog. All three are proved to fire in `tests/unit/test_lint_rules.py`. **Residual:** a dynamic string held in a variable (`msg = f"..."; log.info(msg)`) is not statically detectable and remains a review matter. |
| 12 | 2026-09-16 | Root CI ran `checkov` against `infra/` only, while this server's Terraform lands under `servers/table-mcp/terraform/modules/`, so the gate would have passed vacuously. | Security lead | **Resolved 2026-09-20** — `.github/workflows/ci.yml` now runs checkov twice, over `infra/` and over `servers/` (recursive, so it covers `servers/*/terraform/modules/*`). Two steps rather than one glob: the action passes `directory` straight to `--directory`, which does not expand globs. |
| 13 | 2026-09-16 | UC comments, tags, `constraints` and `generated_expression` are source-controlled text that reaches Glue parameters (§7) and, via `StrategyDecision` rationale text, a Bedrock agent's context. Agree a marking (`x-untrusted`) and a control-character strip before any of it reaches Glue, a log or a prompt. | Security lead | **Resolved 2026-09-20 — SPEC v0.3.2 §14 row 26.** `sanitize.py` added: `sanitize_source_text()` strips control and zero-width characters, normalises to NFC and caps at 1,024 chars with the truncation marked; it runs at the model boundary via the `UntrustedText` annotation, so no model can hold raw UC text. `mark_untrusted()` applies the `<x-untrusted src="uc:{field}">` marker for model contexts. Rationale strings are template + enum only; source text travels in `source_metadata`. |
| 14 | 2026-09-16 | Root rule 2 ("never write to source") is enforced by review only. Agree a mechanical gate — semgrep/import-linter banning `write_deltalake`, `DeltaTable.merge/update/delete`, `.write.` against a source URI, and Databricks SQL execution outside `discover.py`. | Security lead | **Resolved 2026-09-20 — SPEC v0.3.2 §14 row 27.** `policies/nowrite.semgrep.yml` (5 call-site rules) and `policies/.importlinter` (3 contracts) are wired into CI, with `tests/unit/test_nowrite_policies.py` proving each rule fires on a violation and that the real tree stays clean. |
| 15 | 2026-09-20 | `pyspark` pinned at 3.5.3 carried PYSEC-2026-3966 / CVE-2026-32773 (Spark History Server XSS, fixed in 3.5.8), which CI's HIGH/CRITICAL-only Trivy gate would not have caught. Does bumping the pin risk drifting from the EMR Serverless runtime? | Lead architect | **Resolved 2026-09-20** — pin kept at **3.5.8**. EMR Serverless supplies its own Spark runtime, so only the **minor** version (3.5) needs to align; the local pin governs the test runs and the `spark_jobs/` API surface. The EMR release label is pinned in Terraform at Session 5 (`modules/table-mcp-emr`), which is where runtime alignment is actually asserted. |
